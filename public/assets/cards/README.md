# Card Art

Each SVG in this folder maps to a card id from `src/game/cards.js`.

Replace any file with custom art using the same filename, for example:

```text
uk-spitfire-wing.svg
de-tiger-tank.svg
common-supply-line.svg
```

The game client loads card art from `/assets/cards/<card-id>.svg`.
